# Core scientific stack
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1

# NLP / LLMs
transformers==4.44.2
accelerate==0.33.0
sentencepiece==0.2.0
tokenizers==0.19.1

# PyTorch (install the recommended Mac command from pytorch.org first if needed)
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0

# spaCy (for raw transcript work)
spacy==3.7.5
# After installing:
#   python -m spacy download en_core_web_sm

# Experiment tracking / data versioning
mlflow==2.14.2
dvc==3.50.1
dvc-gdrive==3.0.1  # or another remote backend you prefer

# Plotting/analysis
matplotlib==3.9.0
seaborn==0.13.2

# Stats / reliability
statsmodels==0.14.2

# YAML / templating / CLI
pyyaml==6.0.2
jinja2==3.1.4
typer==0.12.3
tqdm==4.66.5
rich==13.9.1

# Fine-tuning (LoRA / QLoRA) – note: bitsandbytes requires CUDA, not Apple Silicon
peft==0.13.0
bitsandbytes==0.45.0
datasets==3.0.2
